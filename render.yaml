# Render Blueprint – static site (no Docker)
# Connect this repo in Render: New → Blueprint → select this repo
services:
  - type: web
    name: nobrainer
    runtime: static
    buildCommand: npm install && npm run build:static
    staticPublishPath: ./dist
